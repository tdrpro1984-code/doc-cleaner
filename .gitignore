# Credentials — never commit these
.env
secrets.json
config.json

# Output files (keep the module code: output/__init__.py, output/markdown.py)
output/*.md
output/*/
test-output/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/

# macOS
.DS_Store

# Spectra (internal workflow, not for public distribution)
.spectra/
openspec/
.claude/skills/spectra-*/

# Spectra / Claude Code (local dev config, not part of the project)
.spectra/
openspec/
.claude/
CLAUDE.md
